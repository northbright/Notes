# "quasar build" with "--debug" option

## Problem
* Need to debug errors in console
* Click the .js file which shows error and the codes are compiled

## Solution
Run "quasar build" with "--debug" option.

```bash
quasar build --debug
```
