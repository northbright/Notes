# Got "xx packages are looking for fundings" when Run npm install

## Problem
* Run `npn install`
* Got

  > xx packages are looking for fundings

## Root Cause
* `npm install` command succeeded and the message are NOT error or warning.

## References
* ['X npm packages are looking for funding' on npm install](https://bobbyhadz.com/blog/npm-packages-are-looking-for-funding)
