# Keep Logs before Redirection in Edge Dev Tools

## Problem
* Need to analyse logs in Edge Dev Tools
* Can not see the log for `/login`(POST) because it'll redirect if it succeeded to login

## Solution
* In Dev Tools Settings > Network > Settings Pane > Select "Preserve Log"（保留日志)
